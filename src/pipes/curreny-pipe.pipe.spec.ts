import { CurrenyPipePipe } from './curreny-pipe.pipe';

describe('CurrenyPipePipe', () => {
  it('create an instance', () => {
    const pipe = new CurrenyPipePipe();
    expect(pipe).toBeTruthy();
  });
});
