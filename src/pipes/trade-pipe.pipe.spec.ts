import { TradePipePipe } from './trade-pipe.pipe';

describe('TradePipePipe', () => {
  it('create an instance', () => {
    const pipe = new TradePipePipe();
    expect(pipe).toBeTruthy();
  });
});
